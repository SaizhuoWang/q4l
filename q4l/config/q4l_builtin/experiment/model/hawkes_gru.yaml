defaults:
  - base_temporal_model

name: HawkesGRU

components:
  temporal:
    name: HawkesGRU
    module_path: q4l.model.zoo.temporal.rnn.hawkesGRU
    kwargs:
      # input_size: ${experiment.model.input_size}
      hidden_size: 256
      # num_layers: 2
      # # rnn_hidden_unit: 64
      # dropout: 0.0
